import React from "react";
import { dataText } from "../../dataMenu/textData";
import Page from "../../components/Page";

const Presentation: React.FC = () => {
  return <Page dataText={dataText} />;
};

export default Presentation;
